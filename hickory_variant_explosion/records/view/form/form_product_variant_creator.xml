<odoo>
    <data>
        <record id="product_variant_creator_form" model="ir.ui.view">
            <field name="name">product.variant.creator.form</field>
            <field name="model">product.variant.creator</field>
            <field name="arch" type="xml">
                <form>
                    <sheet>
                        <field name="template_id" invisible="1"/>

                        <div class="oe_title">
                            <label for="template_name" class="oe_edit_only" string="Product Template"/>
                            <h1>
                                <field name="template_name" readonly="True"/>
                            </h1>
                        </div>

                        <group>
                            <p>The creation method listed below is how you are going to generate the variants. You have the option to create all possible
                                variants based on the attributes defined in the product, or you can be more selective and only generate certain variants using
                                the given Attributes table.
                            </p>
                        </group>
                        <group>
                            <field name="method"/>
                        </group>

                        <group>
                            <notebook attrs="{'invisible': [('method', '!=', 'selective')]}">
                                <page string="Attributes">
                                    <field name="matrix_line_ids" string="Attributes" widget="one2many_list" context="{'show_attribute': False}" nolabel="1"/>
                                </page>
                            </notebook>
                        </group>

                        <footer>
                            <button name="run" string="Updates Variants" type="object" class="oe_highlight"/>
                            or
                            <button string="Cancel" special="cancel"/>
                        </footer>
                    </sheet>
                </form>
            </field>
        </record>
    </data>
</odoo>
